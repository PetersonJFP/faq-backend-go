version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries"
    schema: "sql/schema"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true # <-- minusculas na hora de montar o json, por exemplo: "question" ao invés de "Question"