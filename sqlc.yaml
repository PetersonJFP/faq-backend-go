version: "2"
sql:
  - engine: "postgresql"
    schema: "faq/schema.sql"
    queries: "faq/queries.sql"
    gen:
      go:
        package: "db"
        out: "faq/db"
        emit_json_tags: true
